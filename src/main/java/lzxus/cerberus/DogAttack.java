package lzxus.cerberus;

import org.bukkit.entity.Entity;
import org.bukkit.entity.EntityType;
import org.bukkit.entity.Player;
import org.bukkit.entity.Wolf;
import org.bukkit.event.Listener;
import org.bukkit.event.entity.EntityChangeBlockEvent;

import java.util.Hashtable;

public class DogAttack implements Listener {
    /*private static Hashtable<Wolf, Qu> pList = new Hashtable<String, Player>(); //Key is UUID, Value is Player

    private static void determineBehavior(){

    }

    public static void onBlockChanged(EntityChangeBlockEvent event)
    {
        Entity e = event.getEntity();
        if (e.getType().equals(EntityType.WOLF))
        {
            Wolf w = (Wolf) nearby;
            Player p = PlayerWolfData.isPlayerPet(w);
            if (p!=null)
            {

            }
        }
    }*/
}
